/*数据格式转换器 */
//日期处理
function dateCon(dateStr) {
  if (!dateStr || dateStr == '') return '';
  //getDate()获取当前时间
  var oldDate = getDate(dateStr);
  var oldStr = ''
  var now = getDate();
  if (now.getFullYear() - oldDate.getFullYear() > 0) {
    oldStr = oldDate.getFullYear()+"年"+oldDate.getMonth()+"月"+oldDate.getMonth()+"日";
  } else {
    if (now.getMonth() - oldDate.getMonth() > 0) {
      oldStr = oldDate.getFullYear()+"年"+oldDate.getMonth()+"月"+oldDate.getMonth()+"日";
    } else {
      if (now.getDate() - oldDate.getDate() > 3) {
        oldStr = oldDate.getFullYear()+"年"+oldDate.getMonth()+"月"+oldDate.getMonth()+"日";
      } else {
        if (now.getDate() - oldDate.getDate() == 3) {
          oldStr = '3天前';
        } else if (now.getDate() - oldDate.getDate() == 2) {
          oldStr = '2天前';
        } else if (now.getDate() - oldDate.getDate() == 1) {
          oldStr = '24小时前';
        } else {
          oldStr = '刚刚';
        }
      }
    }
  }
  return oldStr;
}

//姓名处理
function nickNameCon(nickName){
  if(nickName.length>10){
   console.log("测试！！！"+nickName.substring(0,15));
   return nickName.substring(0,15)+"..."
  }
  return nickName;
}

//话题标题处理
function topicTitleCon(topicTitle){
  // console.log("传进来的内容长度："+topicTitle.length)
  if(topicTitle.length>16){
   console.log("测试！！！"+topicTitle.substring(0,16));
   return topicTitle.substring(0,16)+"..."
  }
  return topicTitle;
}
//话题内容处理
// function topicContextCon(topicContext){
//   if(topicContext.length>70){
//     return topicContext.substring(0,70)+'...'
//   }
//   return topicContext;
// }

//文本内容处理
function contextCon(text,setLength){
  console.log("setLength:"+setLength);
  if(text.length>setLength){
    return text.substring(0,setLength)+'...'
  }
  return text;
}


//通过话题内容判断是否显示更多
function isMore(text,setLength){
  console.log("text的length："+text.length);
  console.log("text的length："+setLength);
  if(text.length>setLength){
    return true;
  }
  return false;
}

/*导出模块 */
module.exports = {
  dateCon: dateCon,
  nickNameCon:nickNameCon,
  topicTitleCon:topicTitleCon,
  contextCon:contextCon,
  isMore:isMore
}